<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="10dp"
    android:background="@drawable/bg_eink_card"
    android:orientation="vertical"
    android:padding="12dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_vertical"
        android:orientation="horizontal">

        <TextView
            android:id="@+id/tv_card_num"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:fontFamily="monospace"
            android:text="01"
            android:textColor="@color/eink_text_primary"
            android:textSize="14sp"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/tv_card_title"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="8dp"
            android:layout_weight="1"
            android:text="재귀 하강 계산기"
            android:textColor="@color/eink_text_primary"
            android:textSize="16sp"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/tv_card_difficulty"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="★☆☆☆☆"
            android:textColor="@color/eink_text_primary"
            android:textSize="12sp" />
    </LinearLayout>

    <TextView
        android:id="@+id/tv_card_summary"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="6dp"
        android:lineSpacingExtra="2dp"
        android:text="토크나이저와 재귀 하강 파서로 우선순위·괄호를 처리하는 계산기"
        android:textColor="@color/eink_text_secondary"
        android:textSize="13sp" />

    <TextView
        android:id="@+id/tv_card_concepts"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:ellipsize="end"
        android:maxLines="1"
        android:text="[토큰화] [재귀 하강 파서] [constexpr] [nullptr]"
        android:textColor="@color/eink_text_primary"
        android:textSize="11sp" />
</LinearLayout>
